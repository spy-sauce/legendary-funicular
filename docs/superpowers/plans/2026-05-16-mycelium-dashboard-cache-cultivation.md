# Mycelium Dashboard + Cache-Network Cultivation — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the Mycelium dashboard (operator console with cryogenic-aesthetic canvas + analytics) AND the cache-network runtime (`cache-layer` biome + micro-agent fan-out + cache-hit accounting) as ONE parallel cultivation, dynamically decomposed by `mycelium plant` from a single brief.

**Architecture:** Dogfood — the framework cultivates its own extension. Brief → `mycelium plant` (auto-generates HYPHAs + NUTRIENTS skeleton + mycelium.yaml) → operator hand-edits NUTRIENTS frozen sections → `mycelium contracts freeze` → `mycelium cultivate -c 30` (parallel leaf execution at framework-decided concurrency) → `mycelium harvest -t 0.8` → validate against v9.4 prototype + design doc → PR. No Sean Patrick gate per operator direction.

**Tech Stack:** TypeScript (Node 18+, Commander.js, `@anthropic-ai/claude-agent-sdk`) · Three.js r161 (canvas viz) · vanilla DOM (operator chrome) · YAML (`theme.yaml`, `mycelium.yaml`) · JSONL (event stream) · `npm link` for local dev install.

**Reference artifacts to keep open during execution:**
- Prototype: `.superpowers/brainstorm/4035-1778891130/content/hybrid-v9.4-cache-relays.html` — visual ground truth
- Design doc: `docs/cache-network-micro-agents.md` — cache-network thesis + frozen-vocab additions (`MICRO`, `CACHE_NET`)
- Spec snapshot: `docs/superpowers/specs/2026-05-15-mycelium-dashboard-design.md` — partially stale, palette/aesthetic only
- Audit-run precedent: `hyphae/HYPHA-AUDIT-*.md` + `NUTRIENTS.md` (current root) — pattern for HYPHA shape + NUTRIENTS contracts

---

## File Structure

**Operator authors (this plan's deliverables):**
- `cultivations/dashboard-cache/brief.md` — high-bandwidth scope input for `mycelium plant`
- `cultivations/dashboard-cache/mycelium.yaml` — organism contract (hand-edited after plant)
- `cultivations/dashboard-cache/NUTRIENTS.md` — frozen contracts (skeleton from plant, hand-edited sections)
- `cultivations/dashboard-cache/hyphae/HYPHA-*-AGENT.md` — biome HYPHAs (auto-generated by plant, operator may polish)
- `docs/superpowers/plans/2026-05-16-mycelium-dashboard-cache-cultivation.md` — this file

**Framework leaves produce (cultivate emits, not in plan):**
- `cli/src/commands/dashboard/{index,init,serve,render}.ts` — new CLI command surface
- `cli/src/lib/dashboard/{state,events,theme}.ts` — data layer (consumes sporenet/state.json + JSONL events)
- `cli/src/lib/cache-network/{index,store,keys,accounting}.ts` — cache runtime
- `cli/src/lib/micro-agents/{spawn,registry}.ts` — micro-agent fan-out under leaves
- `templates/dashboard.html` — Three.js canvas + DOM chrome (derived from v9.4 prototype, theme-driven)
- `templates/theme.default.yaml` — default theme emitted by `mycelium dashboard init`
- `DEVELOPER_GUIDE.md` — append-only edits documenting `mycelium dashboard` + cache-network
- `impl/python/mycelium/cache_network.py` + `impl/java/.../CacheNetwork.java` — embedding-side cache adapters (optional, only if leaves choose to ship them)

**Why a sub-directory?** Avoids stomping on the existing root-level `mycelium.yaml` / `NUTRIENTS.md` / `hyphae/` which scope the active ddp-integration + audit-run cultivations. New cultivation lives at `cultivations/dashboard-cache/`; CLI commands accept `--cultivation-dir` or default to cwd. If the framework doesn't yet support that flag, Task 1 includes adding it (small surgical patch).

---

## Task 1: Branch + scaffold the cultivation directory

**Files:**
- Create: `cultivations/dashboard-cache/` (directory)
- Create: `cultivations/dashboard-cache/.gitkeep`
- Verify: `cli/src/commands/plant.ts`, `cli/src/commands/contracts.ts`, `cli/src/commands/cultivate.ts`, `cli/src/commands/harvest.ts` all accept a `--cwd` or `--cultivation-dir <path>` flag, OR cwd resolves relative paths.

- [ ] **Step 1: Create branch**

```bash
git checkout main
git pull
git checkout -b feat/dashboard-cache-net
```

- [ ] **Step 2: Create cultivation directory**

```bash
mkdir -p cultivations/dashboard-cache
touch cultivations/dashboard-cache/.gitkeep
```

- [ ] **Step 3: Verify CLI accepts cultivation directory or cwd-relative paths**

```bash
cd cultivations/dashboard-cache
mycelium plant --help 2>&1 | head -20
```

Expected: help text shows brief/HYPHA paths are relative to cwd, OR a `--cultivation-dir` / `--cwd` flag exists. If neither, stop here and add a minimal `--cwd <path>` option to `plant.ts`, `contracts.ts`, `cultivate.ts`, `harvest.ts` that `process.chdir`'s before resolving paths. Single ~10-line surgical patch; do not refactor.

- [ ] **Step 4: Commit scaffold**

```bash
cd /Users/spy/mfautomation/repos/legendary-funicular
git add cultivations/dashboard-cache/.gitkeep
git commit -m "[MYC] cultivation: scaffold cultivations/dashboard-cache/ for parallel dashboard+cache-net build"
```

---

## Task 2: Write the cultivation brief

**Files:**
- Create: `cultivations/dashboard-cache/brief.md`

This brief is the single high-bandwidth input to `mycelium plant`. Plant generates HYPHAs + NUTRIENTS skeleton + mycelium.yaml from it. Write it richly — the quality of decomposition tracks the quality of the brief.

- [ ] **Step 1: Write the full brief**

Create `cultivations/dashboard-cache/brief.md` with exactly this content:

````markdown
# Brief — Mycelium Dashboard + Cache-Network Cultivation

## Purpose

Ship two interlocked subsystems in one parallel cultivation:

1. **Dashboard** — operator console rendering the live state of any Mycelium cultivation as a 3D canvas (Three.js wireframe Bloch-sphere agents on a fibonacci globe) + operator chrome (metric strip, alert ribbon, event feed, inspect panel).
2. **Cache-network** — framework runtime layer: each leaf fans out into 2-4 micro-agents that share a cache. Cache hits replace redundant LLM/API calls. Hit-rate + entries + calls-saved are first-class metrics. The dashboard renders the cache topology as **dedicated cache relay nodes on an inner radius**, with slow directional pulses showing agent→relay queries.

## Why now

- Dashboard is the framework's UX surface. Static `sporenet/index.html` doesn't show the organism breathing; the dashboard does.
- Cache-network is the framework's call-reduction layer. Per `docs/cache-network-micro-agents.md`: shared cache between micros → fewer calls → less downstream DB development (cache absorbs the read pattern). The `cache-layer` biome named in `docs/bus-router-livegrid-architecture-pack.md` line 154 finally gets built.
- They ship together because the dashboard is the legibility surface for the cache-network. Building one without the other leaves either an invisible runtime or a mock-data UI.

## Visual ground truth

`.superpowers/brainstorm/4035-1778891130/content/hybrid-v9.4-cache-relays.html` is the canonical visual reference. Leaves working on canvas/console MUST read it. Key surfaces locked in v9.4:

- **Cryogenic palette** — `--ink #060912`, `--ice #E8EEF5`, `--cryo #7AE5FF` accent; lifecycle hues desaturated/cool; Geist 200/300 display + Geist Mono data + Cormorant Infant italic marginalia.
- **Per-agent identity colors** — each biome agent has a stable hue from a 10-color identity palette. Lifecycle state (germ/grow/flow/fruit/dorm) modulates brightness/saturation, not hue. Palette in v9.4 `BIOME_IDENTITY` map.
- **Cache relay nodes** — 4-6 cyan-teal `#1E9EBF` octahedrons on inner radius 2.6 (vs agent radius 4.6). Each biome agent edges to its 3D-nearest relay; relays form a 2-neighbor mesh.
- **Slow pulse discipline** — every animation ≥ 1.5s. Packet traversal 6s, dashflow 4s, hit-ring 1.4s, miss-ring 2.0s, biome breath ~0.9 rad/s. No fast blinks.
- **Adaptive zoom** — three presets (globe d=14 / agents d=7.5 / leaves d=2.8) with smooth tween.
- **Multiverse view** — `view: 'multiverse' | 'product'`. Mini-globes per product on outer layer; click to dive into a product's full canvas.
- **Star Wars HUD** — targeting reticle, scan beam, depth HUD on focus.
- **Inspect panel** — click-to-pin per-agent and per-leaf detail; subagent mini-diagram inside.

## Architectural ground truth

`docs/cache-network-micro-agents.md` is the canonical runtime reference. Key contracts:

- **MICRO** — 2-4 per leaf, spawn when leaf transitions to `active`, die when leaf transitions to `done`/`failed`. Color-coded `--life-germ` (cheap routing) vs `--cryo` (full-model routing).
- **CACHE_NET** — keyed by `(tool_name, normalized_args, contract_hash)`. LRU eviction, capacity configurable (default 256 entries per cultivation). Per-leaf invalidation on heal-loop iteration advance. Cache lookup precedes Router classification (not after). Bus emits aggregate cache pulses, not per-call events.
- **HIT_HALO is NOT a contract** — purely visual, lives in dashboard biomes only.

## Required outputs

### Dashboard cultivation outputs

- New CLI command `mycelium dashboard {init,serve,render}` registered in `cli/src/commands/dashboard/`.
- `mycelium dashboard init` writes a default `theme.yaml` into the cwd cultivation, plus a static `dashboard.html` snapshot.
- `mycelium dashboard serve --port 3333` runs an HTTP server that re-reads `sporenet/state.json` + `theme.yaml` per request and re-renders the canvas-driving HTML on the fly. Mirror existing `mycelium sporenet serve` live-render behavior.
- `mycelium dashboard render` writes a static one-shot HTML to `sporenet/dashboard.html`.
- `theme.yaml` per-cultivation moldability: palette (cryo accent + ink + ice family), brand (title + tagline), biome→identity-color mapping, lifecycle modulator curves.
- `templates/dashboard.html` is the template — derives from v9.4 prototype but reads `theme.yaml` at server-render time. Inline Three.js (CDN or vendored) acceptable.
- Adaptive-zoom canvas, fibonacci agent globe, cache relay inner shell, subagent map in inspect panel, multiverse view, slow-pulse discipline. **NOT a re-design** — port v9.4 surfaces faithfully and bind to live data.

### Cache-network runtime outputs

- `cli/src/lib/cache-network/` module: `store.ts` (in-process LRU Map with capacity), `keys.ts` (deterministic key derivation), `accounting.ts` (hits/misses/entries counters + JSONL emission).
- `cli/src/lib/micro-agents/spawn.ts` — given a leaf, spawn 2-4 micros with cheap/full routing split (deterministic per leaf id; e.g., 70% cheap when leaf is non-critical, 50/50 when critical).
- Integration with `cli/src/commands/cultivate.ts` — at the leaf-call chokepoint (around line 480), check cache before SDK call. On hit, emit a `cache.hit` JSONL event + return cached payload. On miss, fall through to SDK + record into cache.
- New `cache.hit` / `cache.miss` / `cache.evict` JSONL event types — register in event schema (NUTRIENTS §3).
- Sporenet state.json extension — add optional `cache: { hits, misses, entries, capacity }` block. Optional means existing readers don't break.
- Embedding-side adapters (`impl/python/mycelium/cache_network.py` + `impl/java/.../CacheNetwork.java`) are NICE-TO-HAVE, not required for v1. If a leaf chooses to ship them, follow existing embedding-package shape.

### Docs outputs

- `DEVELOPER_GUIDE.md` gets append-only sections: "Dashboard" + "Cache-Network". Existing content untouched.
- `docs/cache-network-micro-agents.md` is reference-only — leaves should LINK to it, not duplicate it.
- New top-level doc `docs/dashboard-theming.md` explaining `theme.yaml` schema with worked example.

## Constraints

- **No new npm deps for cache-network.** Use Node stdlib `Map` for LRU. If a leaf needs a real LRU library, document why in FRUIT_READY.
- **Three.js OK as CDN** in templates/dashboard.html (consistent with prototype). Do not vendor unless a leaf has strong justification.
- **Preserve `sporenet/state.json` shape.** Only add optional fields. See `cli/src/commands/sporenet.ts:34-42` for current shape.
- **Preserve cultivate.ts public contract.** Cache-network plugs in at the SDK-call chokepoint only; no new flags, no prompt-shape change, no CommitQueue change.
- **Frozen vocab additions:** `MICRO`, `CACHE_NET` enter the canonical Mycelium vocabulary. Do not redefine `HYPHA` / `BIOME BUS` / `NUTRIENTS` / `FRUITING BODY`.
- **No tests for the framework repo itself** (rule 5). Cultivated apps may have tests; the framework is gated by `tsc --noEmit` + `cultivate --dry-run`.

## Routing hints (for `mycelium plant`)

Likely biome decomposition — plant may refine:

- `dashboard-theme` — `theme.yaml` schema + loader + default theme
- `dashboard-data` — sporenet/state.json reader + JSONL tailer → unified `DashboardState`
- `dashboard-canvas` — Three.js canvas (agents + leaves + cache relays + pulses)
- `dashboard-console` — DOM chrome (metric strip, event feed, inspect panel, alert ribbon)
- `dashboard-cli` — `mycelium dashboard {init,serve,render}` Commander wiring
- `dashboard-docs` — DEVELOPER_GUIDE.md append + new docs/dashboard-theming.md
- `cache-network-store` — `cli/src/lib/cache-network/{store,keys,accounting}.ts`
- `cache-network-integration` — cultivate.ts chokepoint integration + JSONL event types + state.json extension
- `cache-network-micros` — `cli/src/lib/micro-agents/spawn.ts` + registry
- `cache-network-docs` — DEVELOPER_GUIDE.md append + cross-link to design doc

Plant may merge or split these. Operator may polish HYPHA files post-plant.

## Ship target

7-10 days wall-clock from contract-freeze. Audit-run precedent: 22 leaves in 332s wall on `-c 30`. Expect similar ratio.

## Reference reading order for leaves

Every leaf reads, in order:
1. `cultivations/dashboard-cache/NUTRIENTS.md` (frozen contracts)
2. Its own `hyphae/HYPHA-<biome>-AGENT.md`
3. The relevant section of this brief.md (cited by HYPHA)
4. Visual leaves: `.superpowers/brainstorm/4035-1778891130/content/hybrid-v9.4-cache-relays.html`
5. Cache leaves: `docs/cache-network-micro-agents.md`
````

- [ ] **Step 2: Commit the brief**

```bash
git add cultivations/dashboard-cache/brief.md
git commit -m "[MYC] cultivation: brief for dashboard+cache-net parallel build"
```

---

## Task 3: Run `mycelium plant` against the brief

**Files:**
- Generated: `cultivations/dashboard-cache/mycelium.yaml`
- Generated: `cultivations/dashboard-cache/NUTRIENTS.md` (skeleton)
- Generated: `cultivations/dashboard-cache/hyphae/HYPHA-*-AGENT.md` (one per biome)
- Generated: `cultivations/dashboard-cache/CELLULAR-MAP.md`

- [ ] **Step 1: Dry-run plant to inspect prompt**

```bash
cd cultivations/dashboard-cache
mycelium plant --brief brief.md --dry-run
```

Expected: prints the planner prompt + projected biome count. No files written. Skim the prompt — does it cite the brief's biome hints? If the prompt looks garbled, fix the brief, do NOT proceed.

- [ ] **Step 2: Run plant for real**

```bash
mycelium plant --brief brief.md --name dashboard-cache
```

Expected: ~30-90s SDK call. Writes mycelium.yaml + NUTRIENTS.md (skeleton) + hyphae/HYPHA-*.md + CELLULAR-MAP.md into `cultivations/dashboard-cache/`. Exit 0.

- [ ] **Step 3: Inspect what plant generated**

```bash
ls -la hyphae/
cat mycelium.yaml
head -50 NUTRIENTS.md
```

Expected: 8-12 HYPHA files (plant may merge/split the brief's 10 hints). `mycelium.yaml` has `organism:` block with `name: dashboard-cache`, `routing:` with neighbor lists per biome. `NUTRIENTS.md` has section skeletons (`## 1. Theme schema`, etc.) but bodies are TODO/placeholder.

- [ ] **Step 4: Commit the plant output**

```bash
cd /Users/spy/mfautomation/repos/legendary-funicular
git add cultivations/dashboard-cache/
git commit -m "[MYC] cultivation: mycelium plant generated $(ls cultivations/dashboard-cache/hyphae/ | wc -l | tr -d ' ') HYPHAs from brief"
```

---

## Task 4: Hand-edit NUTRIENTS frozen sections

**Files:**
- Modify: `cultivations/dashboard-cache/NUTRIENTS.md`

Plant emits skeletons; operator writes the frozen contract bodies. Every leaf reads these as truth.

- [ ] **Step 1: Author NUTRIENTS §1 — Theme schema**

Replace the §1 skeleton in `cultivations/dashboard-cache/NUTRIENTS.md` with:

````markdown
## 1. Theme schema — per-cultivation `theme.yaml`

**Location:** `cultivations/<name>/theme.yaml` (cwd-relative). Loaded by `cli/src/lib/dashboard/theme.ts` via `js-yaml` (already a transitive dep via `yaml`); fallback to `templates/theme.default.yaml` if missing.

```yaml
# theme.yaml — example
brand:
  title: "mycelium · dashboard-cache"
  tagline: "decentralized · cryogenic · live"
  logo_char: "Ψ"

palette:
  ink:    "#060912"
  ink_2:  "#0A0E18"
  ice:    "#E8EEF5"
  ice_2:  "#A8B4C5"
  cryo:   "#7AE5FF"
  rule:   "#1A2030"

lifecycle:
  germ:  "#B7A8E8"
  grow:  "#7BC9C0"
  flow:  "#8BB8E0"
  fruit: "#D9C390"
  dorm:  "#5A6471"

severity:
  warn:  "#D9B85A"
  alert: "#E08A5C"
  crit:  "#DB5C6E"

identity:
  # biome_id → stable hue. lifecycle state modulates brightness, not hue.
  "dashboard-canvas":   "#7AE5FF"
  "dashboard-console":  "#C9A84C"
  "cache-network-store": "#1E9EBF"
  # ... operator extends per cultivation

modulator:
  pending_brightness: 0.30
  active_brightness:  1.00
  fruit_brightness:   0.90
  failed_brightness:  0.45
  active_pulse_hz:    0.9     # slow-pulse discipline; max 1.5
```

**TypeScript shape (exported from `cli/src/lib/dashboard/theme.ts`):**

```ts
export interface DashboardTheme {
  brand: { title: string; tagline: string; logo_char: string };
  palette: Record<"ink"|"ink_2"|"ice"|"ice_2"|"cryo"|"rule", string>;
  lifecycle: Record<"germ"|"grow"|"flow"|"fruit"|"dorm", string>;
  severity: Record<"warn"|"alert"|"crit", string>;
  identity: Record<string, string>;  // biome_id → hex
  modulator: {
    pending_brightness: number;
    active_brightness: number;
    fruit_brightness: number;
    failed_brightness: number;
    active_pulse_hz: number;
  };
}
```

**Loader contract:** `loadTheme(cwd: string): DashboardTheme` — reads `theme.yaml`, deep-merges over `templates/theme.default.yaml`, validates with a Zod schema, throws on malformed input. Synchronous (called per-render-request).
````

- [ ] **Step 2: Author NUTRIENTS §2 — DashboardState shape**

Replace the §2 skeleton with:

````markdown
## 2. DashboardState — unified shape consumed by canvas + console

**Producer:** `cli/src/lib/dashboard/state.ts:buildDashboardState(cwd)`. Reads `sporenet/state.json` + tails JSONL events in `events/`. Synchronous read at server-render time.

**Consumers:** canvas renderer (`templates/dashboard.html` script section) reads via inlined `window.__DASHBOARD_STATE__`. Console DOM reads same.

```ts
export interface DashboardState {
  organism: {
    name: string;
    started_at: string;        // ISO-8601
    iter: number;              // heal-loop iteration count
    view: "multiverse" | "product";
    active_product_id?: string;
  };
  agents: AgentState[];        // one per biome
  cache: CacheState;
  events: EventEntry[];        // last 200, newest first
  alerts: AlertEntry[];        // active only
  providers: ProviderState[];
}

export interface AgentState {
  id: string;
  state: "pending"|"active"|"fruit"|"failed"|"dorm";
  paused: boolean;
  contracts: string[];          // NUTRIENTS sections owned
  provider: string;             // "anthropic" | "openai" | ...
  leaves: LeafState[];
  history: AgentSnapshot[];     // per heal-loop iter
  cost_usd: number;
  tokens: number;
}

export interface LeafState {
  id: string;
  state: "pending"|"active"|"done"|"failed";
  scope: string;
  files: string[];              // produced artifacts
  commit: string;               // git sha or ""
  duration_seconds: number;
  tokens: number;
  failure_reason?: string;
  micros: MicroState[];         // 2-4 per leaf
}

export interface MicroState {
  idx: number;
  routing: "cheap" | "full";    // cheap = haiku-class, full = opus-class
  last_call_was_hit: boolean;
  fire_count: number;
}

export interface CacheState {
  hits: number;
  misses: number;
  entries: number;
  capacity: number;
  recent_keys: string[];        // last 16 keys, redacted
}

export interface EventEntry {
  ts: string;                   // ISO-8601
  category: "lifecycle"|"nutrient"|"cost"|"health"|"anomaly"|"cache";
  source: string;               // biome_id or "operator"
  severity: "info"|"warn"|"alert"|"crit";
  short_type: string;           // e.g., "cache · hit"
  message: string;
}

export interface AlertEntry { severity: "warn"|"alert"|"crit"; message: string; ts: string; }
export interface ProviderState { id: string; up: boolean; latency_ms: number; model: string; }
export interface AgentSnapshot { iter: number; state: AgentState["state"]; tokens: number; cost_usd: number; }
```

**Builder contract:** `buildDashboardState(cwd: string): DashboardState` is synchronous and pure given the same on-disk state. Re-reads on every call (no caching).
````

- [ ] **Step 3: Author NUTRIENTS §3 — Event schema additions**

Replace the §3 skeleton with:

````markdown
## 3. Event schema — new cache events

Extends the existing JSONL event stream emitted to `events/<organism>.jsonl`. Existing event types unchanged.

```ts
export type CacheEvent =
  | { type: "cache.hit";   ts: string; leaf_id: string; key_hash: string; saved_tokens: number; saved_usd: number }
  | { type: "cache.miss";  ts: string; leaf_id: string; key_hash: string }
  | { type: "cache.evict"; ts: string; key_hash: string; reason: "lru"|"iter_invalidate"|"contract_change" };
```

**Emitter:** `cli/src/lib/cache-network/accounting.ts:emitCacheEvent(event, eventsPath)`. Appends JSONL line, fsync-on-batch (per existing event emitter shape in `cli/src/lib/telemetry/`).

**Tailer:** `cli/src/lib/dashboard/events.ts:tailEvents(path, sinceTs)`. Returns last N events newest-first. Filters by category for the `cache` chip in feedbar.

**Key hashing:** `key_hash` is the first 12 chars of sha256(`tool_name | normalized_args | contract_hash`). Operators see prefix only — never raw args (may contain prompts).
````

- [ ] **Step 4: Author NUTRIENTS §4 — Cache-network runtime contract**

Replace the §4 skeleton with:

````markdown
## 4. Cache-network runtime — `CacheStore` + `MicroAgent` + integration

**Location:** `cli/src/lib/cache-network/index.ts` exports:

```ts
export interface CacheStore {
  get(key: string): CacheEntry | undefined;
  set(key: string, payload: unknown, tokenCost: number): void;
  invalidateLeaf(leafId: string): void;
  invalidateIter(iter: number): void;
  size(): number;
  capacity(): number;
  stats(): { hits: number; misses: number; entries: number };
}

export interface CacheEntry {
  key: string;
  payload: unknown;
  hits: number;
  added_at: string;
  last_hit_at: string;
  token_cost: number;       // tokens saved on each hit
  leaf_id: string;          // origin leaf
  iter: number;             // origin iter — invalidated on advance
}

export function makeCacheStore(opts: { capacity: number }): CacheStore;
export function cacheKey(input: { tool_name: string; args: unknown; contract_hash: string }): string;
```

**Eviction:** LRU. On `set` when at capacity, evict the entry with the oldest `last_hit_at`. Emit `cache.evict` with reason `lru`.

**Iter invalidation:** when heal-loop advances iter, call `invalidateIter(prevIter)` from the cultivate iter-advance handler. Evicts all entries with matching `iter`. Emit `cache.evict` with reason `iter_invalidate`.

**Integration point (cultivate.ts):** at the SDK-call chokepoint (currently around `cli/src/commands/cultivate.ts:480` — leaf may need to relocate if file changed), wrap the SDK call:

```ts
// pseudocode — leaf must read actual file before editing
const key = cacheKey({ tool_name: "claude-agent-sdk", args: prompt, contract_hash });
const hit = store.get(key);
if (hit) {
  emitCacheEvent({ type: "cache.hit", ts: nowIso(), leaf_id, key_hash: key.slice(0,12), saved_tokens: hit.token_cost, saved_usd: hit.token_cost * UNIT_COST });
  return hit.payload;
}
emitCacheEvent({ type: "cache.miss", ts: nowIso(), leaf_id, key_hash: key.slice(0,12) });
const payload = await sdkCall(prompt);
store.set(key, payload, payload.usage?.input_tokens ?? 0);
return payload;
```

**Disable flag:** `--no-cache` on `mycelium cultivate` skips the wrapper entirely (pure passthrough). Default ON.

**MicroAgent fan-out:** `cli/src/lib/micro-agents/spawn.ts` exports `spawnMicros(leaf: LeafSpec): MicroState[]`. Deterministic per `leaf.id` (use `xxhash` of id as seed). Spawn 2-4 micros (uniform random in seeded range). Routing split: 70% `cheap` / 30% `full` for non-critical leaves; 50/50 for critical (severity `critical` in finding context).

**Bus visibility:** cache events publish to the JSONL stream (existing bus) but NOT per-call — aggregate every 1.4s (matching dashboard's emit throttle). Per-call would flood. Aggregate event:

```ts
{ type: "cache.pulse"; ts: string; window_seconds: 1.4; hits: number; misses: number; net_saved_usd: number }
```

Dashboard tailer prefers `cache.pulse` for ticker, raw `cache.hit`/`cache.miss` for canvas pulse animations.
````

- [ ] **Step 5: Author NUTRIENTS §5 — Sporenet state.json extension**

Replace the §5 skeleton with:

````markdown
## 5. Sporenet state.json — additive `cache` block

**Constraint:** existing fields (per `cli/src/commands/sporenet.ts:34-42`) MUST remain present and unchanged. New `cache` block is optional — readers gate on `state.cache != null`.

```ts
// addition only; existing fields elided
interface SporenetState {
  // ... existing fields ...
  cache?: {
    hits: number;
    misses: number;
    entries: number;
    capacity: number;
    last_updated: string;     // ISO-8601, written on each pulse aggregate
  };
}
```

**Writer:** `cli/src/lib/cache-network/accounting.ts:writeCachePulse(stateDir)`. Reads current state.json, deep-merges new `cache` block, atomic temp/rename (existing pattern in `cultivate.ts:writeLeafState`).

**Reader:** dashboard `buildDashboardState` reads `state.cache` if present; treats absence as `{hits:0, misses:0, entries:0, capacity:0}`.
````

- [ ] **Step 6: Author NUTRIENTS §6 — Dashboard CLI surface**

Replace the §6 skeleton with:

````markdown
## 6. Dashboard CLI surface — `mycelium dashboard {init,serve,render}`

**Registration:** `cli/src/commands/dashboard/index.ts` exports `registerDashboard(program: Command): void`. Wire into `cli/src/index.ts` main program.

**Subcommands:**

```
mycelium dashboard init [--cwd <path>] [--force]
  - Writes <cwd>/theme.yaml (from templates/theme.default.yaml) if missing.
  - Writes <cwd>/sporenet/dashboard.html (static one-shot snapshot).
  - --force overwrites existing theme.yaml.

mycelium dashboard serve [--port <n>] [--cwd <path>]
  - HTTP server on port (default 3334; sporenet is 3333).
  - GET / → re-reads state.json + theme.yaml per request, renders templates/dashboard.html with state inlined as window.__DASHBOARD_STATE__.
  - GET /events/stream → SSE stream of new JSONL events since connection time.
  - GET /static/* → serves templates/ assets.
  - Logs each request with ms timing.

mycelium dashboard render [--cwd <path>] [--out <path>]
  - One-shot static render to <cwd>/sporenet/dashboard.html (or --out path).
```

**Render function:** `cli/src/lib/dashboard/render.ts:renderDashboard(state, theme, templatePath): string`. Pure: state + theme → HTML string. No DOM, no fs reads. Inlines state as `<script>window.__DASHBOARD_STATE__ = ${JSON.stringify(state)}</script>` immediately before the closing `</body>`.

**Template:** `templates/dashboard.html` is a single self-contained HTML file. Inline `<style>` + `<script>`. Three.js loaded from CDN `https://unpkg.com/three@0.161.0/build/three.module.js`. Same shape as v9.4 prototype but reads `window.__DASHBOARD_STATE__` and `window.__DASHBOARD_THEME__` at boot instead of using the simulator.

**Template substitution:** the simulator block in v9.4 (the BIOMES/PRODUCTS/ROUTING constants + biomeTick simulator) is REPLACED in templates/dashboard.html with a binding to `window.__DASHBOARD_STATE__`. The render pipeline (canvas Bloch wires, micros, cache relays, packets, inspect panel) is preserved as-is.
````

- [ ] **Step 7: Author NUTRIENTS §7 — Frozen vocab additions**

Replace the §7 skeleton with:

````markdown
## 7. Frozen vocab additions

Two new terms enter the canonical Mycelium vocabulary. Both are case-sensitive uppercase identifiers.

- **MICRO** — a sub-leaf worker. 2-4 per leaf. Spawned at leaf `active`, retired at leaf `done`/`failed`. Routes between `cheap` and `full` providers. Not a separate process — bookkeeping only. Visible on canvas as small spheres orbiting each leaf.
- **CACHE_NET** — the framework-level cache layer. Shared across all biomes in a cultivation. Keyed by tool+args+contract-hash. LRU-evicted. Iter-invalidated on heal-loop advance. Emits hit/miss/evict events on the BIOME BUS.

`HIT_HALO` is REJECTED as vocab — purely visual, lives in dashboard biome internals only. Do not promote to canonical vocab.

Existing vocab (HYPHA, HYPHAE, NUTRIENTS, BIOME BUS, FRUITING BODY, SPORULATION, PRUNING, HPP, NFA) unchanged.
````

- [ ] **Step 8: Verify NUTRIENTS sections cover the brief**

```bash
cd cultivations/dashboard-cache
grep -c "^## " NUTRIENTS.md
```

Expected: ≥ 7 sections. Read NUTRIENTS.md end-to-end. Every `XXX`/`TODO`/`placeholder` from the plant skeleton must be replaced with concrete content. If any biome HYPHA cites a section number, that section must exist.

- [ ] **Step 9: Commit NUTRIENTS**

```bash
cd /Users/spy/mfautomation/repos/legendary-funicular
git add cultivations/dashboard-cache/NUTRIENTS.md
git commit -m "[MYC] cultivation: NUTRIENTS frozen sections 1-7 for dashboard+cache-net"
```

---

## Task 5: Hand-edit mycelium.yaml organism contract

**Files:**
- Modify: `cultivations/dashboard-cache/mycelium.yaml`

- [ ] **Step 1: Set organism block and upgrades**

Open `cultivations/dashboard-cache/mycelium.yaml`. Ensure the top-level `organism:` block reads exactly:

```yaml
organism:
  name: dashboard-cache
  ship_target: 7-10 days
  health_pulse_interval: 30
  harvest_threshold: 0.8
  cellular: true
  gating: contract-freeze
  max_depth: 3
  upgrades:
    - hypha-validator
    - depth-3-enforcement
    - routing-map
    - crash-recovery
```

Leave plant's auto-generated `routing:` block as-is unless a biome neighbor list is obviously wrong (e.g., `cache-network-store` not edged to `dashboard-data`). If you edit routing, prefer ADDING edges to REMOVING.

- [ ] **Step 2: Commit organism contract**

```bash
git add cultivations/dashboard-cache/mycelium.yaml
git commit -m "[MYC] cultivation: organism contract — ship 7-10d, threshold 0.8, gating contract-freeze"
```

---

## Task 6: Freeze contracts

**Files:**
- Modify: `cultivations/dashboard-cache/NUTRIENTS.md` (frozen marker)
- Modify: `cultivations/dashboard-cache/mycelium.yaml` (frozen marker)

- [ ] **Step 1: Run freeze**

```bash
cd cultivations/dashboard-cache
mycelium contracts freeze
```

Expected: validates NUTRIENTS.md sections + mycelium.yaml routing graph, writes a `<frozen at YYYY-MM-DDTHH:MM:SSZ>` marker to NUTRIENTS.md head, sets `gating: contract-freeze` confirmation in mycelium.yaml. Exit 0. If validation fails, fix the cited section and re-run — do NOT bypass.

- [ ] **Step 2: Verify freeze marker present**

```bash
head -5 NUTRIENTS.md | grep -i "frozen"
```

Expected: one line containing `frozen at` + ISO-8601 timestamp.

- [ ] **Step 3: Commit freeze**

```bash
cd /Users/spy/mfautomation/repos/legendary-funicular
git add cultivations/dashboard-cache/NUTRIENTS.md cultivations/dashboard-cache/mycelium.yaml
git commit -m "[MYC] cultivation: contracts frozen at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
```

---

## Task 7: Dry-run cultivate

**Files:**
- (none modified — dry run is read-only)

- [ ] **Step 1: Build the CLI to ensure latest dev image**

```bash
cd cli && npm install && npm run build && cd ..
```

Expected: `tsc` clean, `dist/` produced, exit 0.

- [ ] **Step 2: Run cultivate dry-run**

```bash
cd cultivations/dashboard-cache
mycelium cultivate -c 30 --dry-run
```

Expected: prints planner prompt + leaf list + projected wall-clock estimate. No SDK calls made, no files written. Confirm:
- Every HYPHA in `hyphae/` is represented as ≥ 1 leaf
- No leaf has an empty scope
- Routing edges resolve (no biome cites a neighbor that doesn't exist)
- Estimated wall-clock < 30 min (audit-run precedent: 332s for 22 leaves)

If any of the above fail, fix the relevant HYPHA or NUTRIENTS section and re-run dry-run. Do NOT proceed to real cultivate with a known-bad plan.

- [ ] **Step 3: Save dry-run output for receipt**

```bash
mycelium cultivate -c 30 --dry-run > .dry-run-log.txt 2>&1
ls -lh .dry-run-log.txt
```

Expected: file exists, ≥ 5KB. Not committed (the `.` prefix keeps it out of git).

---

## Task 8: Cultivate

**Files:**
- Generated by leaves (per HYPHA scope). Operator does not write these.

- [ ] **Step 1: Run cultivate**

```bash
cd cultivations/dashboard-cache
mycelium cultivate -c 30
```

Expected: SDK calls fan out at concurrency 30. Per-leaf `🌱 <leaf-id> germinating...` / `🍄 <leaf-id> FRUIT_READY (N files, Ts)` lines stream. Wall-clock target ≤ 30 min for ≤ 30 leaves. Exit 0 if all leaves succeed.

- [ ] **Step 2: Monitor — watch state.json updates**

In a second terminal:

```bash
cd cultivations/dashboard-cache
watch -n 2 'jq ".leaves | map({id, status}) | group_by(.status) | map({status: .[0].status, count: length})" sporenet/state.json'
```

Expected: as cultivate progresses, counts shift from `pending` → `active` → `done`. If any leaf flips to `failed`, note its id — Task 9 handles re-plant.

- [ ] **Step 3: Capture cultivate log**

```bash
cd cultivations/dashboard-cache
# (cultivate log is on stdout from Step 1; redirect there if you want a file)
mycelium cultivate -c 30 2>&1 | tee logs/cultivate-$(date +%Y%m%dT%H%M%S).log
```

(If you already ran Step 1 without `tee`, this is a re-run — only do it if the first run had failures.)

- [ ] **Step 4: Check exit code and FRUIT_READY count**

```bash
jq '[.leaves[] | select(.status == "done")] | length' sporenet/state.json
jq '[.leaves[] | select(.status == "failed")] | length' sporenet/state.json
```

Expected: `done` count == total leaves, `failed` count == 0. If failed > 0, proceed to Task 9. Otherwise jump to Task 10.

---

## Task 9: Heal-loop on failures (conditional)

**Files:**
- (re-runs cultivate against only-failed biomes)

Only execute this task if Task 8 produced any `failed` leaves. Otherwise skip.

- [ ] **Step 1: Identify failed biomes**

```bash
cd cultivations/dashboard-cache
jq -r '.leaves[] | select(.status == "failed") | .biome_id' sporenet/state.json | sort -u
```

Expected: 0-3 biome ids printed. Pipe through `sort -u` to dedupe.

- [ ] **Step 2: For each failed biome, inspect the failure_reason**

```bash
jq '.leaves[] | select(.status == "failed") | {id, biome_id, failure_reason}' sporenet/state.json
```

Read the failure_reason. Two cases:
- **Bug in the leaf's prompt / HYPHA / NUTRIENTS section** — edit the HYPHA or NUTRIENTS section, then proceed to Step 3.
- **Transient SDK failure (timeout, rate limit)** — proceed directly to Step 3.

- [ ] **Step 3: Re-cultivate only the failed biomes**

For each biome `<biome>` from Step 1:

```bash
mycelium cultivate -c 30 --only-biome <biome>
```

Expected: re-runs leaves under that biome. State.json updates per-leaf. Repeat Step 1 until failed count is 0.

- [ ] **Step 4: If a biome fails 3 times in a row, halt and surface to operator**

Three consecutive failures means the HYPHA or NUTRIENTS contract is wrong, not the leaf. Stop cultivate, fix the contract, re-freeze (Task 6), re-cultivate (Task 8).

---

## Task 10: Harvest

**Files:**
- Merges feat branches → main (cultivate's CommitQueue created per-leaf feat branches)

- [ ] **Step 1: Run harvest at threshold 0.8**

```bash
cd cultivations/dashboard-cache
mycelium harvest -t 0.8
```

Expected: reads sporenet/state.json, verifies ≥ 80% leaves are `done`, merges each feat branch into the current branch with `--no-ff`. Exit 0. If threshold not met, harvest refuses — surface to operator.

- [ ] **Step 2: Verify harvest succeeded**

```bash
git log --oneline -20 | head -20
```

Expected: 8-15 new commits on `feat/dashboard-cache-net`, each prefixed with `[MYC/DASHBOARD]`, `[MYC/CACHE]`, `[MYC/DOCS]`, etc. per the orchestrator's auto-tagging.

---

## Task 11: Validate against v9.4 prototype

**Files:**
- Read: `templates/dashboard.html` (just emitted by cultivate)
- Read: `cli/src/commands/dashboard/` (just emitted by cultivate)
- Reference: `.superpowers/brainstorm/4035-1778891130/content/hybrid-v9.4-cache-relays.html`

- [ ] **Step 1: Smoke-test the build**

```bash
cd /Users/spy/mfautomation/repos/legendary-funicular/cli
npm run build
```

Expected: `tsc --noEmit` clean. Exit 0. If type errors, identify offending biome, return to Task 9 with that biome.

- [ ] **Step 2: Smoke-test the CLI command exists**

```bash
mycelium dashboard --help
```

Expected: help text lists `init`, `serve`, `render` subcommands.

- [ ] **Step 3: Init a dashboard in the cultivation dir**

```bash
cd cultivations/dashboard-cache
mycelium dashboard init
ls theme.yaml sporenet/dashboard.html
```

Expected: both files present. `theme.yaml` matches NUTRIENTS §1 shape. `sporenet/dashboard.html` opens in a browser without console errors.

- [ ] **Step 4: Serve and inspect live**

```bash
mycelium dashboard serve --port 3334 &
SERVE_PID=$!
sleep 2
curl -s http://localhost:3334/ | grep -E "window.__DASHBOARD_STATE__|window.__DASHBOARD_THEME__" | head -3
kill $SERVE_PID
```

Expected: curl response includes both window globals. Page renders the canvas when opened in a browser (open `http://localhost:3334/` while serve is still running).

- [ ] **Step 5: Visual parity check against v9.4**

Open both side-by-side in a browser:

- v9.4 reference: `file:///.../legendary-funicular/.superpowers/brainstorm/4035-1778891130/content/hybrid-v9.4-cache-relays.html`
- Built dashboard: `http://localhost:3334/`

Verify ALL of:
- Fibonacci agent globe present, agents have per-identity colors (not lifecycle-uniform).
- 4-6 cache relay octahedrons on inner radius, cyan-teal, breathing slow.
- Agent→relay edges visible; click an agent → nearest relay highlights.
- Multiverse view accessible via toggle (or default product view depending on cultivation state).
- Slow pulse discipline — no animation under 1.5s.
- Inspect panel opens on agent click; subagent mini-diagram present.
- Cache hit % card in cost analytics shows non-zero, climbing during a cultivate run.
- `cache` filter chip in feedbar pivots canvas to relay-emphasis mode.

If any item fails: identify the offending biome (canvas, console, theme, data, cache-network), inspect its emitted files, hand-fix or re-run `mycelium cultivate --only-biome <biome>`.

---

## Task 12: PR + ship

**Files:**
- Push: branch `feat/dashboard-cache-net`
- Create: PR against main

- [ ] **Step 1: Final review of diff**

```bash
cd /Users/spy/mfautomation/repos/legendary-funicular
git log --oneline main..HEAD
git diff main..HEAD --stat | tail -20
```

Expected: 10-30 commits, ~50-150 files changed. New files under `cli/src/commands/dashboard/`, `cli/src/lib/cache-network/`, `cli/src/lib/micro-agents/`, `cli/src/lib/dashboard/`, `templates/dashboard.html`, `templates/theme.default.yaml`, plus `cultivations/dashboard-cache/`.

- [ ] **Step 2: Push branch**

```bash
git push -u origin feat/dashboard-cache-net
```

- [ ] **Step 3: Open PR**

```bash
gh pr create --title "[MYC] dashboard + cache-network cultivation" --body "$(cat <<'EOF'
## Summary

- Ships `mycelium dashboard {init,serve,render}` — cryogenic operator console with Three.js Bloch-sphere agents on a fibonacci globe, cache relay inner shell, multiverse view, slow-pulse discipline. Visual parity with v9.4 prototype.
- Ships `cli/src/lib/cache-network/` runtime — LRU cache wraps SDK calls at cultivate.ts chokepoint, micro-agent fan-out per leaf, hit/miss/evict events on the bus.
- Per-cultivation `theme.yaml` molds the dashboard (palette, brand, identity-color map per biome, slow-pulse modulator).
- Parallel cultivation: framework dynamically decomposed the brief into N HYPHAs via `mycelium plant`, ran them concurrently at `-c 30`.

## Test plan

- [ ] `cd cli && npm run build` clean
- [ ] `mycelium dashboard --help` shows init/serve/render
- [ ] `mycelium dashboard init` in any cultivation writes theme.yaml + dashboard.html
- [ ] `mycelium dashboard serve --port 3334` renders live canvas, agents have per-identity colors, cache relays visible
- [ ] `cache.hit` / `cache.miss` events flow through JSONL → dashboard feedbar's `cache` chip filters them
- [ ] Cultivate any small cultivation with `--no-cache` then without — confirm token savings on the second run

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR URL printed. Open it; CI workflow `ci.yml` should fire (lint/typecheck/test/build per the framework's PR-CI gate).

- [ ] **Step 4: Update memory + handoff**

Append a HANDOFF.md entry summarizing the cultivation (wall-clock, leaf count, any open bugs surfaced). Update `MEMORY.md` index entry for `next-cultivation-dashboard.md` to reflect "shipped via PR #N" status.

---

## Self-review notes

- **Spec coverage:** every section of the brief maps to a NUTRIENTS section (§1-7) and is gated by contract-freeze in Task 6. Cache-network runtime → §3+§4. Dashboard CLI → §6. Theme moldability → §1.
- **Placeholder scan:** no TBD / TODO / "fill in later" in the plan. All schema content is concrete. Routing graph is plant-generated (operator validates but does not author from scratch).
- **Type consistency:** `DashboardState`, `CacheStore`, `CacheEntry`, `MicroState`, `CacheEvent` all use consistent names across §2/§3/§4. `cacheKey` signature matches §4 usage.
- **Sequencing:** Task 7's dry-run gates Task 8's real cultivate. Task 11's visual parity check gates Task 12's PR. Task 9 is conditional on Task 8 failures.
- **Open risks:** (a) `mycelium plant` may emit a biome decomposition that mis-groups dashboard + cache work — operator inspects in Task 3 Step 3 and adjusts before freeze. (b) cultivate.ts:480 chokepoint line may have moved since the design doc was written; cache-network-integration biome must `grep -n "sdkCall\|claude-agent-sdk" cli/src/commands/cultivate.ts` to find the real anchor. (c) Three.js CDN load may fail offline; document this in DEVELOPER_GUIDE.md dashboard section as a known limitation, no workaround needed for v1.
