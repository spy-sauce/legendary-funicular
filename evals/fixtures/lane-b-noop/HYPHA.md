# HYPHA — lane-b-noop leaf

> Mycelium Framework — VibeSpace LLC — The network provides.
> Eval fixture: the smallest possible leaf task. Deterministic on purpose.

## Scope

Write exactly **one** file: `hello.md`, in the current working directory.

## Required content

The file must contain, in order:

1. This exact heading on the first line:

   ```
   # Hello from the Mycelium eval
   ```

2. Exactly one sentence below it:

   ```
   The network provides, and this leaf proved the eval runner can hear it.
   ```

## Rules

- Write `hello.md` and nothing else. No extra files, no directories, no code.
- Do not run git.
- When the file is written, you are done.

## KPI gate

`hello.md` exists and is non-empty. That is the entire bar.
