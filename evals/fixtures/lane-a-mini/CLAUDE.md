# eval-lane-a-mini — fixture cultivation

> Mycelium Framework — VibeSpace LLC — The network provides.
> This is an eval fixture, not a real product. Leaves: read NUTRIENTS.md and
> your own `hyphae/HYPHA-<BIOME>-AGENT.md`, produce exactly the files your
> hypha lists, nothing else.

## Rules

1. Do not run git — the orchestrator owns commits.
2. Frozen contracts in NUTRIENTS.md are absolute; the greet return string is
   exact.
3. Stay in scope: docs-agent owns `README.md` + `USAGE.md`; code-agent owns
   `src/`.
4. No dependencies, no package installs, no test frameworks.
