# HYPHA — docs-agent (eval-lane-a-mini)

> Mycelium Framework — VibeSpace LLC — The network provides.
> Eval fixture biome. Small on purpose.

## Scope

Documentation for the greet micro-library. Exactly two files:

1. `README.md` — one short intro paragraph + one usage example showing
   `greet("world")` and its exact return value per NUTRIENTS.md §2.
2. `USAGE.md` — the `greet(name)` signature, the argument, and the exact
   return-string shape from NUTRIENTS.md §2.

## Rules

- Do not write code files — `src/` belongs to code-agent.
- Quote the frozen return string exactly: `"Hello, <name> — the network provides."`

## KPI gates

- Both files exist and are non-empty.
- The frozen return-string shape appears verbatim in each.
